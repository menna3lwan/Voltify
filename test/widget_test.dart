import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Placeholder – real tests are in test/features/ and test/core/.
    expect(1 + 1, 2);
  });
}
